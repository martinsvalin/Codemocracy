%p Log in with RPX
!= rpx_embed :app_name => CONFIG['rpx_app_name'], :return_url => login_url

%p Or send for a login link
= form_tag login_requests_path do
  %p
    %label Email
    = email_field_tag "email"
  %p
    = submit_tag "Send login link"