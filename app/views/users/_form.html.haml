= form_for @user do |f|
  = render :partial => 'layouts/form_errors', :locals => { :errors => @user.errors } if @user.errors.any?

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Spara'
